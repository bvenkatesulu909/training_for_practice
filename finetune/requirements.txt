torch>=2.4
transformers>=4.40          # works on 4.x and 5.x; template output is normalised
pyyaml>=6.0
pytest>=7.4
numpy>=1.24

# GPU-only extras
# bitsandbytes>=0.43        # 4-bit frozen base (QLoRA) — not yet implemented here
# accelerate>=0.30
